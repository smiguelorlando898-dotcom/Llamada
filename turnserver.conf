# Puerto de escucha
listening-port=3478

# Habilitar fingerprints
fingerprint

# Autenticación con usuario/contraseña (estática)
lt-cred-mech
user=admin:mO*061119

# Dominio o identificador
realm=acalled

# Desactivar SQLite (no necesario en Render)
no-sqlite

# Logs básicos hacia stdout (para verlos en Render)
log-file=stdout
simple-log